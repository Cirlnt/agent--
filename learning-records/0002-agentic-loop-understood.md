# Agentic loop 心智建立（含一处关键误区纠正）

2026-09-05。用户在自解释练习中基本讲清了 agentic loop（约 85% 正确），但暴露一个**关键误区**并已纠正。

**Evidence**：用户原话 —— "工具的副作用由*模型里面的函数*执行，模型只负责*传递信息*"（错误）。纠正（模型从不执行工具，只返回 `tool_use` 结构化"点单"；真正副作用发生在调用方代码；模型职责=决策调不调/调哪个/传什么参 + 生成最终文字）后，用户复述正确：**"真正产生副作用的是模型外面的函数代码，模型实际做的事是决策和输出最终答案。"**

**Implications**：
- ZPD 地板已抬高：执行边界（谁执行工具）与循环机制已在**概念层**建立，M2 起不必重讲基础。
- 潜在绊脚点（预测）：① 循环第一步是"程序 → 模型 发请求"的方向感仍弱（用户原表述为"先接收文字信息"）；② 学到 SDK 工具运行器 / 框架自动执行工具时，可能再次混淆"工具由框架跑 ≠ 由模型跑"，需点破"同一点单制"。
- **缺口**：用户尚未（在本记录时刻）报告已亲手跑通 `code/0001-minimal-agent.py`。动手证据未取得 → 按记忆科学，概念（fluency）≠ 持久（storage）。下一课应放在跑通 + 挑战 B（并行多工具）之后，用真实运行经验巩固。

---
## 完成补记（2026-09-05，午后）

**上文唯一缺口已闭合**。用户亲手修改 `code/0001-minimal-agent.py`，加入第二工具 `get_current_time`（`datetime.now()`、空 `input_schema`、注册进 `EXEC`），并报告完成挑战 A（"你好！"观察零工具调用）与挑战 B。

**验证证据（教师实跑该修改后文件）**：输出在同一 assistant 消息内并行请求 `get_weather({'city': '巴黎'})` 与 `get_current_time({})`，随后单条最终答案同时覆盖天气与时间 → 并行工具调用演示成立。挑战 A 为 2 分钟改问观察、无持久痕迹，采信用户报告。

**判定：第一课完成。** 概念层（上文）、自解释、亲手跑通、并行挑战均已取证。

**M2 起跑备注**：用户实现用两条连续 user 消息触发双工具；DeepSeek Anthropic 兼容端点接受该写法，但真 Anthropic 强制 user/assistant 交替（会报 400）。M2 应顺带点破此规范差异，并示范单条组合问题的写法。
